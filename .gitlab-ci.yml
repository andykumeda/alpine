image: node:16

stages:
  - build
  - deploy

build:
  stage: build
  script:
    - npm install
    - npm run generate

deploy:
  stage: deploy
  script:
    - echo 'deploying to pages'
  artifacts:
    paths:
      - /.output/public
  only:
    - alpine-new

